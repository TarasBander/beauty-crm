# Postgres (used by docker-compose and backend/.env)
POSTGRES_USER=crm
POSTGRES_PASSWORD=crm_dev_password
POSTGRES_DB=crm
POSTGRES_PORT=5432

DATABASE_URL="postgresql://crm:crm_dev_password@localhost:5432/crm?schema=public"
