PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=crm
DB_PASSWORD=crm_dev_password
DB_DATABASE=crm

# comma-separated list of allowed CORS origins for the frontend
CORS_ORIGIN=http://localhost:5173

# JWT auth — change JWT_SECRET before this ever leaves your machine
JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=1d

# used only by `npm run seed:admin` (backend workspace)
SEED_ADMIN_EMAIL=admin@beautycrm.local
SEED_ADMIN_PASSWORD=ChangeMe123!
SEED_ADMIN_FIRST_NAME=Admin
SEED_ADMIN_LAST_NAME=User
